{
  "name": "Apotheek de Bongerd",
  "user": "ubuntu",
  "install": "bash .cursor/dev-setup.sh && npm ci && set -a && . ./.env && set +a && npm run db:push && npm run db:seed",
  "start": "bash .cursor/dev-setup.sh",
  "terminals": [
    {
      "name": "dev",
      "command": "set -a && . ./.env && set +a && npm run dev"
    }
  ],
  "ports": [3000]
}
